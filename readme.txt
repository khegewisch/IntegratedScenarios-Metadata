These bash scripts will be run on the netcdf files of the Integrated Scenarios project
to fix the metadata in these files. We are going to agree on making consistent metadata for these files before running these. 

git add --all
git commit -a -m 'message'
git push origin master
